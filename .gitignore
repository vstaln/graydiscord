# deps
node_modules/
dist/
build/
coverage/
.npm/

# env — everything except the example (covers .env.production, .env.staging, ...)
.env*
!.env.example

# data (local sqlite/json persists, keep .gitkeep)
data/*.json
data/*.sqlite*
!data/.gitkeep

# logs
*.log
logs/
nohup.out

# os / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# ts / build caches
*.tsbuildinfo
.turbo/
